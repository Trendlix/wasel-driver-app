enum InboxStatus { offers, support, updates }

enum VerifyOtpType { register }

enum UserAccountStatus { pending, approved, rejected }

enum SortEnum { createdAt, amount }

enum SortOrderEnum { asc, desc }

enum NetworkStatus { online, offline, noInternet }

enum ChatAction { view, reply }
